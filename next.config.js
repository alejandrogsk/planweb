/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  env: {
    GTM_ID: 'GTM-M9R3HFW',
  },
  i18n: {
    locales: ['es'],
    defaultLocale: 'es',
  }
}

module.exports = nextConfig
